version: '3.8'

services:
  api:
    build: .
    container_name: logishift_api
    restart: always
    environment:
      - PORT=3000
      # Вставляем твои реальные данные прямо сюда:
      - DATABASE_URL=postgres://your_app_user:OQ4kktp-Ht5n@postgres_app:5432/smenabot_db
      - JWT_SECRET=mySu32perSecквафыirecячтKeyc322c3!
    networks:
      - smenabot_net
    ports:
      - "3000:3000"

networks:
  smenabot_net:
    external: true
    name: smenabot-stack_default
